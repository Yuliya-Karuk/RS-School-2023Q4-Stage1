import bgDark from './bgDark';
import bgLight from './bgLight';
import mainDark from './mainDark';
import mainLight from './mainLight';

const images = {
  dark: {
    bg: bgDark,
    main: mainDark,
  },
  light: {
    bg: bgLight,
    main: mainLight,
  },
};

export default images;
